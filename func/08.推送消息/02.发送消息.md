

# 01.发消息按钮

将某消息发送到用户的企微或钉钉手机上

## 催办脚本定义

1. 自定义跳转地址：参数 path
2. 不设置path参数时，用默认的跳转地址

发送成功后的回调函数：callback

```js
this.sendMessageClick({
  userIdCol: 'user_id',
  title: '消息标题',
  content: '消息内容，测试中',
  callback:(r)=>{
    console.log(r)
  }
})
```
 